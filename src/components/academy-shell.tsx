import Link from 'next/link';
import { Heart, BookOpen, ArrowUpRight, Sparkles } from 'lucide-react';
import { lessons } from '@/lessons/catalog';
export function AcademyShell({children,active}:{children:React.ReactNode;active?:string}) {
  return <div className="academy"><aside className="rail"><Link className="logo" href="/"><span><Heart fill="currentColor" size={19}/></span>Leet<strong>Love.</strong></Link><div className="rail-caption">YOUR LEARNING SPACE</div><Link className={`rail-home ${!active?'active':''}`} href="/"><BookOpen size={17}/> Learning library</Link><div className="rail-caption path-caption">THE FOUNDATIONS · 5 LESSONS</div><nav aria-label="Learning path">{lessons.map((lesson,i)=><Link key={lesson.id} href={`/learn/${lesson.slug}`} className={`rail-lesson ${active===lesson.id?'active':''}`}><span className="rail-index">0{i+1}</span><span>{lesson.shortTitle}<small>{lesson.pattern}</small></span>{active===lesson.id&&<span className="rail-dot"/>}</Link>)}</nav><div className="rail-note"><Sparkles size={19}/><strong>Small steps. Big aha’s.</strong><p>Choose. Watch. Understand.<br/>Then try it on your own.</p></div><div className="rail-foot">Learn the why. Love the how.</div></aside><div className="academy-body"><header className="academy-top"><Link href="/">Learning library</Link><span className="top-stage">STAGE 02 <i/> The foundations</span><a href="https://leetcode.com/problemset/" target="_blank" rel="noreferrer" className="external-practice">Practice on LeetCode <ArrowUpRight size={14}/></a></header>{children}</div></div>;
}
