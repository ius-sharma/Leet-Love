import Link from 'next/link';
import { Heart, Grid2X2, ArrowUpRight, Sparkles, CirclePlay, ChevronRight } from 'lucide-react';
import { lessons } from '@/lessons/catalog';
export function AcademyShell({children,active}:{children:React.ReactNode;active?:string}) {
 return <div className="academy"><aside className="rail"><Link className="logo" href="/"><span><Heart fill="currentColor" size={19}/></span>Leet<strong>Love</strong><b className="logo-dot">.</b></Link><div className="rail-caption">WORKSPACE</div><Link className={`rail-home ${!active||active==='problem'?'active':''}`} href="/"><Grid2X2 size={17}/> Problem explorer</Link><Link className={`rail-home ${active==='create'?'active':''}`} href="/create"><Sparkles size={17}/> Solution studio</Link><div className="rail-caption path-caption">VISUAL COLLECTION <span>{lessons.length}</span></div><nav aria-label="Visual solutions">{lessons.map(lesson=><Link key={lesson.id} href={`/learn/${lesson.slug}`} className={`rail-lesson ${active===lesson.id?'active':''}`}><CirclePlay size={16}/><span>{lesson.shortTitle}<small>{lesson.pattern}</small></span>{active===lesson.id&&<span className="rail-dot"/>}</Link>)}</nav><div className="rail-note"><span className="note-icon"><Sparkles size={19}/></span><strong>Make your code click.</strong><p>Turn a supported Python solution into a visual walkthrough.</p><Link href="/create">Open solution studio <ChevronRight size={14}/></Link></div><div className="rail-foot"><span className="avatar">Y</span><span>Your workspace<small>Progress saved on this device</small></span></div></aside><div className="academy-body"><header className="academy-top"><Link href="/" className="mobile-brand">LeetLove<span>.</span></Link><Link href="/">Workspace</Link><ChevronRight size={12}/><span>{active==='create'?'Solution studio':active&&active!=='problem'?'Visual solutions':'Problem explorer'}</span><Link className="mobile-studio" href="/create" aria-label="Open solution studio"><Sparkles size={16}/></Link><a href="https://leetcode.com/problemset/" target="_blank" rel="noreferrer" className="external-practice">Open LeetCode <ArrowUpRight size={14}/></a></header>{children}</div></div>;
}
